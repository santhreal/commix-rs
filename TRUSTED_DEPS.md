# Trusted dependencies

Each external dependency uses a caret range in `Cargo.toml` and is audited before it appears here. Path dependencies are internal Santh crates and are not listed.

- base64
- proptest
- serde
- serde_json
- thiserror
- tokio
- tracing
- tracing-subscriber
